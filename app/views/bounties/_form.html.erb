<%= form_with(model: bounty) do |f| %>
  <%= render "shared/errors", errors: bounty.errors %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :amount %>
  <%= f.text_field :amount %>

  <%= f.submit %>
<% end %>
